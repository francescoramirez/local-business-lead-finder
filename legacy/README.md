# Legacy Playwright scraper

Historical, **unsupported** Google Maps UI scraper. It is not part of the installable `leadfinder` package and is excluded from CI.

The supported product is `leadfinder search` with Google Places API (New).
